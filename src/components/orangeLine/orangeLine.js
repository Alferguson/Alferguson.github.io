import React from "react";
import styles from "./orangeLine.module.css";

const OrangeLine = ({ visible, x1, y1, x2, y2 }) => {
  return <div className={styles.orangeLine} />;
};

export default OrangeLine;
